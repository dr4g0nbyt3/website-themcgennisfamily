# Dependencies
node_modules/

# Build outputs
dist/
.astro/

# Environment variables
.env
.env.local
.env.production
.env.development
.env.*.local

# Local development
.DS_Store
*.log
*.swp
*.swo
*~
.vscode/
.idea/

# Temporary files
tmp/
temp/

# Pagefind search index (generated during build)
public/pagefind/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Secrets and credentials
*.pem
*.key
*.cert
secrets.json
credentials.json

# OS generated files
Thumbs.db

# SOP files (internal documentation)
sop-*.md

# Unpublished drafts (underscore-prefixed pages are excluded from the Astro build)
src/pages/**/_*.astro

# Mission trip media — pending subject permission, must not reach the public repo
public/images/trip/

# Mission trip draft — protected in BOTH prefixed and unprefixed states.
# Un-prefixing it makes it buildable/deployable; this keeps it out of the public repo regardless.
src/pages/blog/entrepreneur-ai/first-mission-trip-guadalajara-esports.astro
